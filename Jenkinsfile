pipeline {
  agent { docker { image 'python:3.7.2' } }
  stages {
    stage('Setup workspace'){
      steps{
        sh 'ls -la'
        sh 'mkdir reports/'
      }
    }
    stage('validate') {
      steps {
        sh 'ls -la'
      }
    }
    stage('Test') {
      steps {
        sh 'ls -la'
      }
    }
    stage('Package') {
      steps {
        sh 'ls -la'
      }
    }
    stage('Verify') {
      steps {
        sh 'ls -la'
      }
    }
    stage('Deploy') {
      steps {
        sh 'ls -la'
      }
    }
  }
  }